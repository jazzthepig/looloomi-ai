venv/
__pycache__/
*.pyc
.env
.DS_Store
node_modules/
dashboard/node_modules/
*.log
src/data/cis/cis_history.db
Shadow/

# Local research data — never committed.
# _data/cis_historical/cis_historical_11yr.csv = 12 MB Phase-2 CIS backfill from 2026-07-18.
# Regenerate via scripts/reconstruct_cis_history.py + scripts/cis_historical_ingest.py (Mac-side).
# Other _data/* files are audit / strategy_records working artifacts. Mac-side ownership.
_data/

# Internal operational docs — never commit
JAZZ_TODAY.md
ATTACK_PLAN.md
COMMIT_READY.md
WEEKLY_REVIEW.md
MINIMAX_SYNC.md
dashboard/dist.old*

# Strategy reports — local working files (backtest outputs, correlation drafts, handoffs)
reports/

# ── Mined-strategy research: the SEARCH, not the plumbing (2026-08-12) ──
# Calibration and mining scripts whose output is an edge. Nothing imports these
# at runtime (verified: no importer outside their own smoke tests), so ignoring
# them costs production nothing.
#
# ⚠️ THE LINE IS "IS IT IMPORTED BY TRACKED CODE", NOT "IS IT A STRATEGY".
# Railway deploys from git: a module that is not in git is not on the server.
# `src/data/signals/beta_core_q_overlay.py` and `beta_core_size.py` are imported
# by `src/api/main.py` (:1248, :1310, unguarded — the endpoint would 500), and
# `beta_core_q_hook.py` by `beta_core_paper.py` (:771, inside try/except — it
# would fail SILENTLY and the ⓠ book's 60-day clock would simply stop accruing).
# Those four stay tracked. If the edge must leave git, it leaves as PARAMETERS
# (thresholds, cell tables → Supabase or env), not as import targets.
scripts/backtest_q_override.py
scripts/tests/test_backtest_q_override_smoke.py
src/research/validation/r77_episode_vdb_cluster.py

# ── Agent/harness churn cleanup (2026-06-25) — local operational noise, never commit ──
health-check-*.md
HEALTH_CHECK*.md
health-reports/
health-checks/
monitor-logs/
meditations/
macro_brief_latest.json
PRD*.md
*.bak
*.bak.*
MINIMAX_SYNC_ARCHIVE_*.md
*.db-journal
.claude/settings.local.json
# Agent-local scratch — machine-specific, never shared state (2026-08-01).
# NOTE: .claude/skills/ is deliberately NOT ignored. CLAUDE.md's skills table
# names task-audit and completion-verification as shared discipline every lane
# loads; ignoring them would make each agent's copy private and silently
# divergent — the same "two sources of truth" failure as the HYPE universe.
.claude/worktrees/
# pip mis-install artifacts (files literally named "=x.y.z")
=*

# elizaOS Otter plugin — commit src + character, not deps
elizaos-plugin/node_modules/
elizaos-plugin/dist/

# Conversation archives — local provenance only, never committed.
# 3MB+ of raw session transcripts; regenerate with scripts/export_transcripts.py.
# Curated knowledge lives in PROJECT_STATE / REFUTATION_LEDGER / WEEKLY_REVIEW / docs/.
docs/transcripts/
_data/cis_historical/
