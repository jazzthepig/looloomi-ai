# LP-facing narrative draft — 2026-08-08

*Jazz. Per OVERSIGHT §7.3 ("对外材料对齐等 LP-facing 文档更新待 Jazz 决策") you
authorized this draft. **Sourced entirely from** `docs/P2_SHIPPED_DISCIPLINE_SURFACE_2026-08.md`
§5 source-of-truth chain. Every claim below maps to a specific CI test or commit.
Edit wording before publish; do NOT edit the source-of-truth chain.*

> **Compliance reminder:** positioning language only — STRONG OUTPERFORM / OUTPERFORM /
> NEUTRAL / UNDERPERFORM / UNDERWEIGHT. No BUY / SELL / ACCUMULATE / AVOID.
> HK SFC: this is product positioning, not investment advice.

---

## 1. One paragraph (≈70 words)

> 我们做的是一个 AI 策展的加密 FoF,目前处于**方法论阶段** — 我们最有价值的资产
> 不是任何一条策略,是一套让任何策略主张都必须通过的证伪装置。**第一条方法论
> 干净的前向曲线将于 2026 年 10 月初满 60 天**;在那之前 LP 看到的是 12 项
> 编译进 CI 的纪律门禁 + 110+ 条含机制的台账 + 25.1pp/yr 测量并纠正的幸存者偏差,
> 而不是策略业绩。

## 2. Three things we can say honestly today

### 2.1 纪律编译进了 CI,不写在 PPT 上

14 个 category · 121+ 具名断言 · venue consolidation 18 sub-checks,每一次 push
前都是强制闸门。**这条指向:** `test_strategy_discipline` 13 项 ·
`test_neutralize` 5 项 · `test_strategy_durability` 4 项 · `test_cold_start_contract`
6 项 + 11 个其他 category。来源:`docs/P2_SHIPPED_DISCIPLINE_SURFACE_2026-08.md`
§1 表格。**S-103, S-105, S-92, S-95, S-96, S-104 六个事件各自编译进了对应测试。**

### 2.2 失败被完整记录且可审计

REFUTATION_LEDGER.md 含 S-1 → S-100+ 的逐条撤回与机制说明,S-111/S-112 最新追加。
R76–R94 十五次连败全部留有 refuted verdict + 失败机制 + lesson,没有粉饰。**这条指向:**
S-103(β confounding across tiers)、S-105(executability > returns)、
S-106(0.8% days carry 100% of return)、S-107(funding Sharpe 8.75 measures
payment flow, not P&L)、S-108/S-109(sample-size constraint, not mechanism)、
S-111(25.1pp/yr survivorship)、S-112(admission rule, not short book)。

### 2.3 产品本体 = ①层 hold-the-panel + ⓠ regime override

①层是 FoF core 也是其他 sleeve 的基准。**已上线 1 天,SHIP-ready 2026-10-初** ——
60 天日历门槛不能加速,日历是唯一不能买的资源。**这条指向:** commit
`121b54c` + `922d0c5`(stalled-clock observability)· `test_beta_core_book` 8 项 +
`test_regime_override_enforcer` 23 项。**对照 OVERSIGHT §1.1 的 5 本前向 book 表,
3 本 demote 为研究记录(因同一构造 S-103/S-105 各否定一次),只剩 ①层 + dingge +
two-layer 是产品候选。**

## 3. Three things we cannot say today (the firewall)

| Cannot claim | Why |
|---|---|
| 任何 alpha 数字 | COMPLETENESS_ASSESSMENT §1 — 事件计数未完成前,headline +7.99 不是证据 |
| "我们的信号排序有效" | 同上 — STRONG OUTPERFORM 事件均 +3.58(t=1.55 不显著);OUTPERFORM 反向 t=−3.62 |
| 任何策略"已验证" | 0/8 通过 `oos_survival=True` 的项目自身门槛 |

**这条指向:** COMPLETENESS_ASSESSMENT_2026-08.md §1 + §2。

## 4. What 60 days unlocks (the only forward milestone)

①层 beta_core_paper 60 天满 = **2026-10-初**(calendar-bound)。届时 LP 看到的会是:
- 一条**方法论干净**的前向曲线(无 L/S 构造,无 BTC benchmark 偏差)
- benchmark NAV 与 book NAV 同行标记,excess 是算术,不是事后挑的
- stalled-clock detection + cache-loss recovery 已编译进 CI,曲线可信
- 同时 survivorship-free(687-asset membership, was 75-alive-only)

**这条指向:** `test_beta_core_book` 8 项 · commit `121b54c` + `922d0c5` +
S-111 survivorship backfill · PROJECT_STATE.md OPEN RISK #4。

## 5. What survives scrutiny (the differentiator)

**这条不是营销话,是机制可验证。** 当 LP 尽调时:
- 问"你们的信号排序有效吗" → 答:"中间档无效,底部档反转。详见
  COMPLETENESS_ASSESSMENT §1 的事件计数。"
- 问"你们的策略过了多少关" → 答:"0/8。这是公开台账。"
- 问"为什么你们知道自己的测错了" → 答:"因为 25.1pp/yr 的幸存者偏差是我们
  自己测出来并纠正的;15 次 R-number 连败是我们自己测出来并写明机制的。"

---

## 6. Source-of-truth chain(每次 LP 尽调可直接引用)

| LP claim | Cite this |
|---|---|
| "我们把证伪装置编译进 CI" | `docs/P2_SHIPPED_DISCIPLINE_SURFACE_2026-08.md` §1 表格 + `bash scripts/preflight.sh` |
| "X 次被自己的测量推翻" | `REFUTATION_LEDGER.md` R76–R94 + S-1→S-100+ · 完整可审计 |
| "60 天前向门槛" | `tests/test_strategy_discipline.py::test_executability_floor_is_enforced` + `test_ship_records_carry_the_evidence_floor` |
| "中性化是单位换算" | S-103 + `tests/test_neutralize.py::test_pure_beta_neutralises_to_zero` + `test_real_alpha_survives_neutralisation` |
| "执行性先于回报" | S-105 + `tests/test_strategy_durability.py::test_a_single_failure_is_already_degraded` |
| "事件计数先于 t-stat" | COMPLETENESS_ASSESSMENT §1 ⚠️ 框 + Lesson #81 |
| "产品本体 = ①层 hold-the-panel" | OVERSIGHT §0 + §3 + §7.1 + `tests/test_beta_core_book.py` |
| "calendar-bound,不能加速" | OVERSIGHT §3 P0 理由 + ①层 2026-10-初 满 60 天 |
| "L0 数据架构不再让 asset_class 住在观测行" | `tests/test_data_architecture.py`(4 项)+ S-106 重构 |
| "Mac↔Railway 不漂移" | cis_push `SCHEMA_VERSION=1.0` · vector schema v2 (27-dim) — preflight 每次 echo |

---

## 7. What is NOT in this LP-facing narrative

OVERSIGHT §7.3 写的"待 Jazz 决策"指的是**措辞**;以下**结构性**事项本 draft 不触碰,
需要单独决策:

- ARCHITECTURE.md 主张"最深对象是 Entity/Decision" —— `decisions` 0 行,
  `entities` 1 行。**要么接上,要么从对外材料里拿掉**。COMPLETENESS_ASSESSMENT
  §0 给 F 评级;这是判断题不是工程题。
- MCP on deprecated HTTP+SSE transport(spec `2026-07-28` 弃用,12-month offramp)
  —— 不影响 LP-facing 但 LP 尽调技术审查时会问。
- Asset_embeddings_history 0 行 · risk_meter_history 0 行 —— 决策链的另两端
  也是空的,OPEN RISK #3(b)。

---

*Draft prepared by Seth, 2026-08-08, per Jazz authorization ("都做啊"). All claims
sourced; do not extend this narrative without updating `docs/P2_SHIPPED_DISCIPLINE_SURFACE_2026-08.md`
§5 source-of-truth chain first.*
