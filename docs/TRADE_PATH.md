# TRADE_PATH.md — 真钱交易主干(Hyperliquid,3,000 USDC 试跑)

*Seth,2026-09-23。Jazz 已定:交易所 = Hyperliquid,资金 = 3,000 USDC。*
*§3 已定 A;首版策略见 §3b。*

## §1 主干:5 段,只有这 5 段是 P0

| 段 | 内容 | 状态 |
|---|---|---|
| 1 价格 | 持仓标的的实时价格(HL `allMids` / 盘口) | ✅ HL 采集器已在读 |
| 2 目标权重 | §3b 的 4 币 TSMOM 仓位(每天一次) | ❌ 小件:一个纯函数,输入 HL 日线 |
| 3 执行器 | 读 HL 实盘余额 → 跟目标比出差额 → 下单 → 写 `execution_intents` / `execution_outcomes` | ❌ **唯一要写的** |
| 4 对账 | 用 HL 实盘余额算真实净值,跟模拟净值比,每天出偏差 | ❌ 小件 |
| 5 告警 | Telegram | ✅ 已有 |

**主干之外全部降为 P2**:坏了就标成过期,不挡交易。新模块必须写明服务第几段,写不出来就归研究。

## §2 实测:Hyperliquid 上 ① 的 25 个名字(2026-09-23)

**永续:25/25 全有。** 做多资金费率(`funding_history`,venue=hyperliquid):
近 7 天年化 **+15.5%**,近 30 天年化 **+12.1%**,由多头支付。

**现货:只有 4 个有真实流动性**(24h 成交额,`spotMetaAndAssetCtxs`):

| 名字 | HL 现货代币 | 24h 成交 |
|---|---|---|
| HYPE | HYPE/USDC | $99.9M |
| BTC | UBTC/USDC | $37.4M |
| ETH | UETH/USDC | $17.3M |
| SOL | USOL/USDC | $11.2M |
| AVAX / LINK / AAVE / BNB | UAVAX、LINK0、AAVE0、BNB0 | < $25k,不可用 |
| 其余 17 个 | — | 无现货 |

UBTC / UETH / USOL 是 Unit 桥接的包装代币,多一层桥的托管风险。

## §3 待定:① 在 HL 上用哪种工具

| | A. 4 个现货 | B. 25 个永续 |
|---|---|---|
| 标的 | BTC / ETH / SOL / HYPE | ① 全部 25 个 |
| 持有成本 | 0 资金费 | 年化约 12–15%(3,000U 约每天 $1) |
| 和模拟 ① 的对应 | 不对应,是一本新的 4 币账 | 完全对应 |
| 和 08-23 规则 | 符合「① 持现货」 | **违反**,需要 Jazz 显式改规则 |
| 额外风险 | Unit 桥 | 爆仓线(1x 也要看保证金模式) |

**Jazz 2026-09-23 定 A。** ①-HL = 4 币现货;25 币的模拟 ① 继续跑,作研究基准。
HL 永续留给 Jev 的多空叠加层(Jazz 负责,测试中)。

## §3b 首版策略(S-409 实测后定)

**TSMOM 多/空仓,4 币等权,只用现货。** 每个币的仓位 = 20/60/120 日收益为正的个数 ÷ 3
(0、⅓、⅔、1),t 日收盘出信号,t+1 日收盘成交。

- 两个窗口都把最大回撤砍掉 30–50%,总收益和持有相近(2023-05→:+237% vs +252%)
- 全在现货,不付资金费;不加杠杆
- **空头腿暂不上。** 它只在熊市有用(2022),2023–2025 牛市里拖累;作为独立 sleeve 先模拟盘
- Jev 叠加层的比较基准:「持有 + 0.5×TSMOM 多空」(S-409 T6)

依据与全部数字:REFUTATION_LEDGER §S-409。

## §4 执行器的硬约束

- **Key = HL API 钱包(agent wallet)**:Jazz 在 HL 里自己创建并授权给主账户;
  API 钱包能下单、不能提币。私钥只放执行器运行环境的环境变量里,**不经过任何 Minimax 通道,也不经过 Seth**。
- **运行地点(Jazz 2026-09-24 定):东京或新加坡的小型云主机**(每月 5–20 美元)。不为速度,为两件事:
  key 与 Mac 上的 Minimax 物理隔离;24 小时在线(加永续/杠杆后要盯保证金)。
  执行器写成与机器无关;写库走 Railway `/internal` 接口(本地只有 anon,S-404),Railway 不可用时本地缓存、恢复后补写。
- 价格过期就拒绝交易,不猜价。单笔上限、日换手上限、一键停(Redis 标志位)。
- 只在权重偏离超过阈值时调仓,不每天交易。
- 首版总仓位固定 1.0,不加 ③ 的暴露调节 —— 前两周只测执行成本。

## §5 节奏与判据

| 阶段 | 做什么 | 过关判据 |
|---|---|---|
| 第 1–2 天 | 只算不发:读真实余额和盘口,算出订单,写 `execution_intents`,不提交 | 每天 1 批意图;订单数量和金额人工核对无误 |
| 第 3 天 | 3,000U 真跑 | 首批订单成交或按规则过期,全部落表 |
| 第 3–14 天 | 每日对账 | 实盘和模拟的日跟踪误差;实施缺口(bps)有数 |
| 之后 | Jev 叠加层接 HL 永续(Jazz 负责,测试中) | 由 Jev 自己的 60 天验证协议决定 |

## §6 分工

- **Seth**:执行器、对账。
- **A**:HL 价格与资金费采集保持活着;面板循环(S-408)按 P1 修。
- **B**:每日对账判据和看板。
- **C**:② 的 CIS 倾斜,第 30 天前准备好。
- **Jazz**:Jev 多空叠加层;HL API 钱包与运行地点。
