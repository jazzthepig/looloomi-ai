-- S-365 · applied 2026-09-16 · 「等权持有本 panel」第一次真的存在 + edge_map 双基准
--
-- ┌ 起因 ────────────────────────────────────────────────────────────────┐
-- │ MEMORY.md / CLAUDE.md:基准 = 等权持有本 panel;不是 0,也不是 BTC     │
-- │ (S-103:BTC 基准给每档扣 2.16pp,t=3.96)                             │
-- │ 实测:signal_outcomes.bench 在 7,706/7,743 行上是 BTC                 │
-- │       signal_journal.benchmark_symbol 是 NULL=66 / SPY=50 / BTC=41    │
-- │ **法条写了一年,数据里一行都没有按它算过。**                          │
-- └──────────────────────────────────────────────────────────────────────┘
--
-- Jazz 2026-09-16 定:**A/B,两套都报,看效果。**
--
-- ── 四个迁移 ───────────────────────────────────────────────────────────
-- s365   panel_ew_forward_returns 视图
-- s365b  统一视图带 a_ret/bench_trail;edge_map 加 benchmark 维 + 去重 + 唯一键
-- s365c  refresh_signal_edge_map 读统一视图、算两套基准、改 upsert
-- s365d  统一视图归一 signal 拼法
--
-- ── 三次差点报错数,每次都靠同一个问题拦下:这个数在数学上说得通吗 ──────
--
-- ① **面板选错** —— 用 261 个标的的全市场面板比 47 个 CIS 筛选过的标的,
--    面板里 339 个标的信号从没覆盖过。每一档都 +2~+6pp。
--    **那是准入筛选(ADV>$15M),不是技能。**
--    → 面板改为「当天 cis_scores 的加密宇宙」(475 天,100% 覆盖,中位 32 标的,无前视)。
--
-- ② **口径不对齐** —— 拿单资产**买入持有** 30 天收益,比一个**每日再平衡**组合的
--    复利收益。再平衡收割波动,系统性高于个股买入持有的平均,压出 −3~−5pp 的假负值。
--    → 面板收益改为「各成员自己的 30 天买入持有,再等权平均」,与 a_ret 同口径。
--    **中性化/换算是读数前的单位对齐,不是发表前的动作**(S-103)。
--
-- ③ **拼法分裂** —— legacy 写 `STRONG OUTPERFORM`(空格),journal 写
--    `STRONG_OUTPERFORM`(下划线),同值两拼被拆成两行;下划线那行
--    n=5 / alpha 17.91 / **胜率 100%** —— 一个会被当成头条的伪影。
--    → 在统一视图归一(不改源表,记录要保留它当时写了什么)。
--    ⚠️ 只做拼法归一,**不做同义词映射** —— 不在枚举里的值原样透出,
--    悄悄映射成最近的合法值,是把数据问题变成看不见的问题。
--
-- ── 顺带修掉 edge_map 一直带着的两个缺陷 ───────────────────────────────
-- ④ 表上只有 id 主键,**没有业务键唯一约束**,裸 INSERT 每跑一次叠一层。
--    实测每个 (signal, risk_band) 都有 2 行,来自 07-04 与 09-08。
--    不按 computed_at 过滤的消费者拿到同一格的两个时代读数。
--    → 唯一键 (benchmark, signal, risk_band) + upsert。
-- ⑤ 原来直读 `signal_outcomes`,**journal 段整个看不见**。→ 改读统一视图。
--
-- ── A/B 结果(2026-09-16) ──────────────────────────────────────────────
--   signal              n(btc)  α_btc    n(panel)  α_panel   差额
--   STRONG OUTPERFORM     131   +2.91      141     +2.68     -0.24
--   UNDERWEIGHT           301   +0.47      302     +2.22     +1.76
--   OUTPERFORM           1663   -3.20     1885     +1.17     +4.37
--   UNDERPERFORM         4414   -3.39     4756     +0.43     +3.82
--   NEUTRAL               750   -7.66      750     -0.32     +7.34
--
-- 读法:
--   · S-103 的 2.16pp 量级对,但**分布极不均匀**(0.2 ~ 7.3pp)。
--     NEUTRAL 档的读数几乎完全由基准决定;STRONG OUTPERFORM 档几乎不受影响。
--   · **量表中段仍然是倒的**:UNDERWEIGHT(最看空)是第二好的一档。
--     只有最上面一档排序正确。
--   · ⚠️ **这些 n 不是证据量。** 30 天窗口重叠、横截面相关,
--     独立事件数远小于行数。按我们自己的规矩,报 t 之前先报独立事件数
--     (「按天加权的 alpha/t 在事件计数前不是证据,是叙事」)。
--
-- ── 不变量 ─────────────────────────────────────────────────────────────
-- **永远不跨 benchmark 平均。** 唯一键让它在结构上做不到;
-- 统一视图的 comment 也写明了 alpha 的基准跨行不同。

-- 验收:
--   select benchmark, count(*) from signal_edge_map group by benchmark;  -- panel_ew=25, btc=21
--   select public.refresh_signal_edge_map();                             -- 再跑行数不变(幂等)
--   select string_agg(distinct signal,' | ') from signal_edge_map;        -- 五个枚举值,空格版
--   select count(*) from (select benchmark,signal,risk_band from signal_edge_map
--                         group by 1,2,3 having count(*)>1) x;            -- 0
